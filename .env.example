# .env
# Only needed when the targets are not at ../back and ../front (the scaffolded layout),
# e.g. while developing the archetypes side by side.
# BACK_DIRECTORY=../back-express
# FRONT_DIRECTORY=../front-standard
# E2E_BACK_PORT=3000
# E2E_FRONT_PORT=4000
# E2E_SERVER_TIMEOUT_MS=15000
